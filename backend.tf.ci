# The configuration for the `remote` backend.
 terraform {
        backend "remote" {
            organization = var.TF_ORGANIZATION
            workspaces {
                name = var.TF_WORKSPACE
            }
        }
    }
# The configuration for the `remote` backend.
 terraform {
        backend "remote" {
            organization = "${{ env.TF_ORGANIZATION }}"
            workspaces {
                name = "${{ env.TF_WORKSPACE }}"
            }
        }
    }
